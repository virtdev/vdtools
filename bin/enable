#!/usr/bin/python

import sys
from vdtools import enable

def usage():
    print('usage: enable [name]')

if __name__ == '__main__':
    if len(sys.argv) != 2:
        usage()
        sys.exit()
    enable(sys.argv[1])
