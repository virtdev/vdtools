#!/usr/bin/python

import os
import sys
import commands

_name = commands.getoutput('readlink -f %s' % sys.argv[0])
_path = os.path.dirname(_name)
_dir = os.path.dirname(_path)
sys.path.append(_dir)

from lib.common import disable

def usage():
    print('usage: disable [name]')

if __name__ == '__main__':
    if len(sys.argv) != 2:
        usage()
        sys.exit()
    disable(sys.argv[1])
