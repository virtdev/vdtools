#!/usr/bin/python

import sys
from vdtools import disable

def usage():
    print('usage: disable [name]')

if __name__ == '__main__':
    if len(sys.argv) != 2:
        usage()
        sys.exit()
    disable(sys.argv[1])
