#!/usr/bin/python

import sys
from vdtools import link

def usage():
    print('usage: link [src] [dest]')

if __name__ == '__main__':
    if len(sys.argv) != 3:
        usage()
        sys.exit()
    src = sys.argv[1]
    dest = sys.argv[2]
    print(str(link(src, dest)))
