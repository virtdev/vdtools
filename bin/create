#!/usr/bin/python

import sys
from vdtools import create

def usage():
    print('usage: create [type]')

if __name__ == '__main__':
    argc = len(sys.argv)
    if argc != 2:
        usage()
        sys.exit()
    print(str(create(sys.argv[1])))
