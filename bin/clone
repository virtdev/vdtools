#!/usr/bin/python

import sys
from vdtools import clone

def usage():
    print('usage: clone [parent]')

if __name__ == '__main__':
    argc = len(sys.argv)
    if argc != 2:
        usage()
        sys.exit()
    print(str(clone(sys.argv[1])))
