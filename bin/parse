#!/usr/bin/python

import sys
from vdtools.parser import parse

def usage():
    print('usage: parse [path]')

if __name__ == '__main__':
    argc = len(sys.argv)
    if argc != 2:
        usage()
        sys.exit()
    path = sys.argv[1]
    parse(path)
    print('successfully parse %s' % path)
